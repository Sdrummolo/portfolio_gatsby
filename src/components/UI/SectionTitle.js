import styled from "styled-components"

export default styled.h2`
  font-size: 1.5rem;
  font-weight: 700;
  text-transform: uppercase;
  text-align: center;
  color: var(--main);
  margin-bottom: 40px;
`
